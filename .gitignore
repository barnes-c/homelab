.terraform/
.terraform.tfstate.lock.info
*.tfvars
terraform.tfstate
terraform.tfstate.backup

# Committed deliberately: pins exact provider builds and checksums, which the
# version constraints in versions.tf do not.
!.terraform.lock.hcl

# generated
.schematic-id-*
.imager-*
profiles/*.yaml
!profiles/*.yaml.tmpl
images/
_out/
